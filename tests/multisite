#!/usr/bin/env bash
# Runs the suite against a network install. wp-env's `run` passes its argument as
# a single exec token, so this exists to keep the command one word.
exec vendor/bin/phpunit -c phpunit-multisite.xml.dist "$@"
